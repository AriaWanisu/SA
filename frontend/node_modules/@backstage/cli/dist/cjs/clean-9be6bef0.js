'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

require('commander');
require('chalk');
var index = require('./index-dff1c82a.js');
var fs = _interopDefault(require('fs-extra'));
require('@backstage/cli-common');

async function clean() {
  await fs.remove(index.paths.resolveTarget("dist"));
  await fs.remove(index.paths.resolveTarget("dist-types"));
  await fs.remove(index.paths.resolveTarget("coverage"));
}

exports.default = clean;
//# sourceMappingURL=clean-9be6bef0.js.map
